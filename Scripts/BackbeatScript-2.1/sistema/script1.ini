[script]
n0=#nfenix off
n1=on 1:text:*:#parke_geriatriko: {
n2=  if ($nick == nfenix) { 
n3=    if (un usuario configura a MeMo para que no recibia ningún tipo de notificación isin $1-) msg $chan msg MeMo set notify off
n4=    if (un usuario habilita el acceso restringido de la información de un canal atraves de isin $1-) msg $chan msg CHaN set #canal restricted on
n5=    if (/list de la información de un canal atraves de CHaN? Escribe el comando sin /. isin $1-) msg $chan msg CHaN set #canal private off
n6=    if (activa la privacidad para que este no isin $1-) msg $chan msg CHaN set #canal private on
n7=    if (idioma de NiCK para su uso atraves isin $1-) msg $chan msg NiCK set language número
n8=    if (MeMo para que reciba los mensajes cuando se conecte isin $1-) msg $chan msg MeMo set notify on
n9=    if (habilita la seguridad de CHaN de la información de un cana isin $1-) msg $chan msg CHaN set #canal secure on
n10=    if (uso del comando RECOVER atraves de NiCK? isin $1-) msg $chan msg NiCK release nick password
n11=  }
n12=}
n13=#nfenix end
